@using OdeToFood.Core
@model Restaurant

<div class="card mt-2">
    <div class="card-header">
        <h3>@Model.Name</h3>
    </div>
    <div class="card-body">
        <span>Location: @Model.Location</span>
        <span>Cuisine: @Model.Cuisine</span>
    </div>
    <div class="card-footer">
        <a class="btn btn-primary" asp-page="./Detail" asp-route-restaurantId="@Model.Id">
            <i class="fas fa-info-circle"></i>
        </a>
        <a class="btn btn-info" asp-page="./Edit" asp-route-restaurantId="@Model.Id">
            <i class="fas fa-edit"></i>
        </a>
        <a class="btn btn-danger" asp-page="./Delete" asp-route-restaurantId="@Model.Id">
            <i class="fas fa-trash-alt"></i>
        </a>
    </div>
</div>
